services:
  portafolio:
    image: ghcr.io/${IMAGE_NAME:-esteban0406/portafolio}:latest
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
      RESEND_API_KEY: ${RESEND_API_KEY}
    restart: unless-stopped
